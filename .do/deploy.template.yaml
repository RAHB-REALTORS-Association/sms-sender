spec:
  name: twilio-app
  services:
  - name: sms-sender
    git:
      branch: master
      repo_clone_url: https://github.com/RAHB-REALTORS-Association/sms-sender.git
    envs:
    - key: SECRET_KEY
      value: "somesupersecretkey"
      type: SECRET
    run_command: gunicorn -b :8080 app:app
